# Lock fee
CALL_METHOD
    Address("${account}")
    "lock_fee"
    Decimal("${amount}");

CALL_METHOD
    Address("${account}")
    "create_proof_of_amount"
    Address("${resource}")
    Decimal("${amount}");

# Call method
CALL_METHOD
    Address("${component}")
    "update_dapp_definition_account"
    Address("${account}");
    
# Drop all proofs
DROP_ALL_PROOFS;