# Lock fee
CALL_METHOD
    Address("${account}")
    "lock_fee"
    Decimal("${amount}");

# Create proof from fungible owner badge
CALL_METHOD
    Address("${account}")
    "create_proof_of_amount"
    Address("${resource}")
    Decimal("${amount}");

# Call method
CALL_METHOD
    Address("${component}")
    "update_owner_badge"
    Address("${resource}");

# Drop all proofs
DROP_ALL_PROOFS;