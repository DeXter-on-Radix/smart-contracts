# Lock fee
CALL_METHOD
    Address("${account}")
    "lock_fee"
    Decimal("${amount}");

# Create proof from fungible owner badge
CALL_METHOD
    Address("${account}")
    "create_proof_of_amount"
    Address("${resource}")
    Decimal("${amount}");

# Call method
CALL_METHOD
    Address("${component}")
    "emergency_switch"
    Some(Enum<${index}u8>()) # or None
    Some(Enum<${index}u8>()); # or None

# Drop all proofs
DROP_ALL_PROOFS;